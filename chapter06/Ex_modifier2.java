package chapter06;


/*import chapter07.MyParent; //에러 MyParent의 접근제어자가 (default)이기 때문에 같은 패키지에서 가능

class MyChild extends MyParent { //에러 MyParent의 접근제어자가 (default)이기 때문에 같은 패키지에서 가능
    public void printMember() {
        //System.out.println(prv); //에러 private은 같은 클래스에서만 쓸 수 있기 때문에
        //System.out.println(dft); //에러 (default)는 같은 패키지 안에서만 쓸 수 있기 때문에
        System.out.println(prt);
        System.out.println(pub);
    }
}*/

public class Ex_modifier2 {
    public static void main(String[] args){

    }
}
